// SINGLE SOURCE OF TRUTH for backend URL
// Change this one value to switch between local and production

const CONFIG = {
    API_BASE_URL: "http://localhost:8000",  // Change to Railway/Render URL when deployed
    SUPABASE_URL: "YOUR_SUPABASE_URL",
    SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY"
};
